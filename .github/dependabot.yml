version: 2
updates:
  # Keep the SHA-pinned GitHub Actions current (Dependabot updates the pin + comment).
  # A pin is only a control if something moves it when the upstream action is fixed.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns: ["*"]

  # Flag newly-disclosed CVEs / updates in the Python dependency closure.
  - package-ecosystem: pip
    directory: /
    schedule:
      interval: weekly
    groups:
      python:
        patterns: ["*"]
